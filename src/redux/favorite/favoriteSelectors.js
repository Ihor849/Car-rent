export const getFavorite = state => state.favorite;

export const getFavoriteList = state => state.favorite.favoriteCars;

export const getFavoriteRerender = state => state.favorite.reRender;

export const getFavoriteLoading = state => state.favorite.loading;